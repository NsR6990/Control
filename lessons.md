# درس‌ها — دانشی که به هر پروژه می‌خورد

این فایل به هیچ پروژه‌ای بند نیست. هرچه اینجاست از یک اشتباه واقعی یا
یک اندازه‌گیری واقعی درآمده و در پروژهٔ بعدی هم صادق است.

قاعده‌های کار در `CLAUDE.md` کنار همین فایل است. وضعیت هر پروژه در
`projects/<نام>/nazer.md`. این‌جا فقط چیزی می‌آید که **مال هیچ‌کدام
نیست و مال همه است.**

---
---

## نقشهٔ جلسه‌ها

| جلسه | چه می‌بیند | کارش |
|---|---|---|
| ناظر | `Control` و مخزن پروژه | نظارت |
| سازنده | فقط مخزن پروژه | ساخت |

جلسهٔ سازنده اتاق فرمان را نمی‌بیند و نباید ببیند.

**کِی جلسهٔ سازنده را عوض کنیم — تصمیمش با مالک است، نه با ناظر.**
یک جلسهٔ سازنده چند بند پشت سر هم می‌برد؛ بعد از هر merge، `main` کشیده
می‌شود و بند بعدی در همان جلسه ادامه می‌یابد. مالک وقتی عوضش می‌کند که
کانتکست جلسه آن‌قدر پر شده باشد که کیفیتش پایین بیاید.

**ناظر از خودش نگوید «این جلسه دیگر تمام است».** یک بار همین شد:
ناظر بعد از merge بند ۲۴ گفت جلسهٔ `Chortke 1.6` بازنشسته است، در
حالی که آن جلسه تازه باز شده بود و فقط یک بند پشت سرش داشت. مالک
تصحیحش کرد.

نشانه‌های واقعیِ اینکه وقتش رسیده: جلسه حرفی بزند که با سند نمی‌خواند،
یا چیزی بپرسد که سندها جوابش را دارند. آن‌وقت به مالک بگو — تصمیمش
با اوست. هزینهٔ جلسهٔ تازه کم است چون حافظه در مخزن است.

**و پیش‌تر در همین فایل نوشته بود «بعد از هر merge جلسه را عوض کن».
آن غلط بود و `docs/state.md` چرتکه از همان اول خلافش را می‌گفت.**

جلسهٔ ناظر هم همین‌طور: در چرتکه چند بار در طول کار به سقف خورد و پرس
شد. این عادی است و دلیل وجود همین فایل‌هاست.

## چک کوتاه ناظر روی هر PR — اصلش

مالک گفت: وقتی جلسه راه طولانی رفته، به جوابش اعتماد کن و همان توکن را
دوباره نسوزان؛ فقط یک چک کوتاه. اصلش سه چیز است و بیشتر نه:

- **دامنه** — فایل‌های `PR` همه در محدودهٔ بند هستند؟ یک فراخوان
  `git diff --stat`، بدون خواندن دیف.
- **جاهای ممنوع** — فهرستی که همان پروژه برای خودش دارد، دست نخورده؟
- **یکی دو «باید»ِ خودِ همان بند** — چیزی که بریف صریح خواسته یا ممنوع
  کرده، واقعاً همان شد؟

بقیه — آزمایش روی اپ، شات‌ها، منطق — گزارش خود جلسه است.

**و یک چهارمی که از یک ایراد واقعی درآمد — ۱۴ شهریور:**

- **اگر بند میدان تازه‌ای ساخته که از یک لایه به لایهٔ دیگر سفر می‌کند،
  کل مسیرش را دنبال کن — از جمله فایل‌هایی که در دیف نیستند.**

در بند بیست‌وپنج چرتکه، ستون تازه در پایگاه ساخته شد، اعتبارسنجی نوشته
شد، زمان‌بند اصلاح شد، رابط ساخته شد — ولی خطی که مقدار را از بدنهٔ
درخواست به تابع می‌داد **دست نخورد** و میدان تازه را برنداشت. ذخیره
روی سرور زنده همیشه `400` می‌گرفت.

**سه لایه دفاعی هر سه از کنارش رد شدند و هر سه به یک دلیل:** جلسهٔ
سازنده فایل را ندید چون تغییرش نداده بود؛ سی‌وپنج تست سبز نگرفتندش چون
همه تابع را مستقیم صدا می‌زدند و هیچ‌کدام از خودِ مسیر رد نمی‌شد؛ و چک
ناظر ندیدش چون **چک دامنه فقط چیزی را می‌بیند که عوض شده.** این جنس
ایراد در چیزی زندگی می‌کند که عوض نشده.

**قاعدهٔ عمومی که از آن درآمد و در سند تصمیم‌های چرتکه هم نشست:** هر
مرزی که داده از آن رد می‌شود، تستِ خودش را روی همان مرز دارد — نه تست
دو سرِ آن.

**فهرست جاهای ممنوع مال هر پروژه است، نه مشترک.** نمونهٔ کاملش در
`projects/chortke/nazer.md` است؛ از بند ده تا هفده جواب داد و دو بار
چیز واقعی گرفت.

## چطور کانتکست جلسه پر می‌شود — اندازه‌گیری‌شده، ۱۴ شهریور ۱۴۰۵

سه آزمایش در همین مخزن، در یک جلسه:

- پوشه‌ای که `CLAUDE.md` دارد، **اولین بار** که ناظر فایلی از آن پوشه را
  باز کند: هم آن فایل می‌آید هم `CLAUDE.md` ریشه، کامل. حدود هفت
  کیلوبایت.
- **همان پوشه، دفعهٔ دوم:** هیچ.
- پوشه‌ای که `CLAUDE.md` ندارد: هیچ، هیچ‌وقت.

یعنی هزینه یک‌بار به ازای هر پوشه است، نه هر بار خواندن. گشتن در مخزن
گران نیست.

**ساختار همین مخزن روی همین نشسته:** فایل وضعیت هر پروژه `nazer.md`
است، نه `CLAUDE.md`. چون هیچ سازوکار خودکاری به این اسم کار ندارد، فقط
وقتی خوانده می‌شود که خودمان بگوییم — بعد از اینکه معلوم شد جلسه مال
کدام پروژه است.

این رفتار **امروزِ** ابزار است و می‌تواند عوض شود. نقشه عمداً طوری چیده
شده که به آن بند نباشد.

## کار موازی دو جلسهٔ ناظر — قاعده‌ای که از یک برخورد واقعی درآمد

دو جلسهٔ ناظر روی دو پروژه **می‌توانند** همزمان باز باشند. هر جلسه
ماشین خودش را دارد و به آن یکی نشت نمی‌کند؛ اندازه‌گیری شد — مخزن
پروژهٔ دیگر اصلاً روی دیسک نبود.

خطر از سمت خواندن نیست، از سمت **نوشتن** است: هر دو در یک مخزن
می‌نویسند.

**چه شد:** ۱۴ شهریور، جلسهٔ ناظر نلمان `PR` تفکیک را باز کرد. همان
ساعت‌ها جلسهٔ ناظر چرتکه یازده `PR` دیگر مرج کرد. وقتی `PR` نلمان
دوباره نگاه شد، **۵۴ کامیت عقب و تداخل‌دار** بود و دفتر چرتکه در آن
فاصله ۳۷ کیلوبایت رشد کرده بود. اگر merge می‌شد، آن کار برمی‌گشت عقب.

**قاعده‌ها:**

- **دفتر پروژهٔ خودت آزاد است.** `projects/<نام>/` را هر وقت خواستی
  بنویس؛ دو جلسه که فایل‌های جدا را دست می‌زنند بی‌تداخل merge می‌شوند.
- **فایل‌های مشترک — `CLAUDE.md`، `lessons.md`، `README.md` — قفلِ
  نانوشته دارند.** پیش از نوشتن در این‌ها از مالک بپرس جلسهٔ ناظر
  دیگری باز است یا نه.
- **پیش از باز کردن `PR` روی کنترل، `main` را تازه بگیر.** یک
  `git fetch` ارزان است؛ `PR` عقب‌افتاده گران است.
- **`PR` باز روی کنترل را زیاد نگه ندار.** هرچه بیشتر باز بماند، شانس
  اینکه زیرش عوض شود بیشتر است.
- **تغییر در فایل مشترک، تنها در `PR` خودش می‌رود.** با دفتر پروژه
  قاطی نشود. یک `PR` تک‌موضوعیِ چنددقیقه‌ای عملاً نمی‌تواند به چیزی
  بخورد؛ یک `PR` بزرگ که ساعت‌ها باز بماند، می‌تواند.

### ترتیب merge اهمیت ندارد — این را به مالک هم بگو

مالک ۱۴ شهریور پرسید: «الان تو می‌گویی merge بزن، ده دقیقه بعد جلسهٔ
ناظر دیگری همین را می‌گوید. مشکل‌ساز است؟» جواب **نه** است، و سه دلیل
دارد که هر سه سازوکاری‌اند نه سلیقه‌ای:

- **گیت‌هاب نمی‌گذارد چیزی بی‌صدا خراب شود.** اگر دو شاخه واقعاً به هم
  بخورند، دکمهٔ merge خاموش می‌شود و تداخل را اعلام می‌کند. بدترین
  حالت «نشد» است، نه «شد و خراب کرد».
- **دو جلسهٔ ناظر تقریباً فایل مشترکی ندارند.** هرکدام فقط در
  `projects/<نام>/` خودش می‌نویسد. سطح برخورد فقط همان سه فایل ریشه
  است.
- **گیت سه‌طرفه merge می‌کند.** اگر دو شاخه دو جای متفاوتِ یک فایل را
  عوض کنند، هر دو تغییر می‌مانند. برگشتن کار دیگری فقط از
  `force-push` یا از حل‌کردنِ بدِ تداخل می‌آید، نه از merge عادی.

**پس آنچه ۱۴ شهریور خراب شد، merge نبود — `PR`ی بود که ساعت‌ها باز
مانده بود.** درمانش زودتر بستن است، نه دیرتر merge کردن.

## چهار قاعدهٔ کاری برای حرف‌زدن با مالک

- **دستور برای مالک همیشه یک خط است، با `&&`، بی‌کامنت.** چسباندن
  چندخطی در `Termius` روی آی‌پد به یک خط می‌ریزد، و `#` اول خط بقیه را
  کامنت می‌کند — یک بار کل نصب `cloudflared` همین‌طور بی‌صدا نرفت
  (`command not found`).
- **پیش از فرستادن هر یک‌خطی، نقل‌قول‌ها را بشمار.** ۱۴ شهریور دو بار
  پشت سر هم این رعایت نشد: یکی `'` اضافه داشت، یکی `` ` `` اضافه. هر
  دو بار شل مالک روی `>` ماند و هیچ‌چیز اجرا نشد، و او پرسید «چرا داری
  ضعیف عمل می‌کنی؟» — حق داشت. **افت کیفیت به شکل ندانستن نمی‌آید؛ به
  شکل سرسری‌خواندنِ چیزی می‌آید که جلوی چشم است.** شمردنِ `'` و `"` و
  `` ` `` پنج ثانیه است.
- **در دستور، مالک را وادار به انتخاب هویت نکن.** برچسب‌هایی مثل «با
  روت بزن» یا «با کاربر فلان بزن» برای او یک کارِ قابل‌انجام نیست؛ گفت
  «وقتی می‌گی با روت من نمی‌فهمم». دستور را طوری بنویس که خودش داخلش
  هویت را عوض کند (`incus exec ... -- su - user -c '...'`)، و اگر لازم
  شد یک بار ساده توضیح بده دو حساب چه فرقی دارند — بعد دیگر تکرارش نکن.
- **شات مالک را بخوان، نه اینکه متن بخواهی.** او با شات کار می‌کند و
  گفت: «به چیزی گیر الکی نده، من مجبورم شات بگیرم». نشانه‌ای در دستور
  بگذار (`APP HTTP 200`، `NGINX-OK`) که در شات با یک نگاه پیدا شود.


---
---

## کارنامهٔ اشتباه‌ها

*این بخش را جدی بگیر. الگویش از خود اشتباه‌ها مهم‌تر است.*

۱. گفتم «مکث‌شده» در سند مدل داده هست؛ نبود
۲. تست نرخ ارز را روی مسیر غلط زدم و ۵۰۲ گمراه‌کننده گرفتم
۳. جدول کنتراست را غلط شمردم — گفتم ۲۸ از ۲۹، واقعیت ۲۹ از ۳۱ بود
۴. گفتم نشانهٔ ۴×۴ را روی آی‌پد نگاه کن، در حالی که اپ هیچ‌جا مستقر
   نیست و مالک راهی برای بازکردنش نداشت
۵. خلاصهٔ کامیت را با `head -20` بریدم و از خروجی بریده نتیجه گرفتم
۶. از «تیپ شاخه تکان نخورده» نتیجه گرفتم کار push‌نشده هست، در حالی که
   مدرکِ نقضش در خلاصه‌ای بود که چند دقیقه قبلش خوانده بودم
۷. در بریف `[متن اینجا می‌آید]` گذاشتم، در حالی که مالک متنش را امضا
   کرده بود
۸. سه متن هشدارآمیز نوشتم جایی که یک خط خواسته شده بود. مالک اسمش را
   گذاشت: «همون وسواسی که تو داری میره توی اپ»
۹. **تحقیق عمیق را بدون برآورد هزینه راه انداختم.** شکل ابزار را
   می‌دانستم — راستی‌آزمایی خصمانه یعنی ضرب‌شدن تعداد ایجنت‌ها — و باز
   هم عدد نگفتم. سه‌ونیم میلیون توکن و ۱۰۷ ایجنت. از این درآمد
   قاعدهٔ هفت.
۱۰. **دربارهٔ کتابخانهٔ کامپوننت متناقض حرف زدم.** اول گفتم کتابخانه
   ظاهر را درست می‌کند، بعد گفتم ظاهر از قراردادهای طراحی می‌آید.
   مالک گفت «توی پاسخ قبلیت من گیج شدم» — و حق داشت.
۱۱. **سه دور لوگوی ردشده.** دور سوم بدترینش بود: از روی عکسی که
   نمی‌توانستم ردیابی‌اش کنم، دستی بازسازی کردم و مارپیچ پایین‌چپ
   غلط درآمد. وقتی فایل واقعی را گرفتم و با `potrace` ردیابی کردم،
   همان کار در یک دور درست شد. **حدس‌زدن از روی تصویر، اندازه‌گیری
   نیست.**

۱۲. **جلسه را از نقشهٔ مالک منحرف کردم.** نقشه‌اش روشن بود: مخزن
   کنترل ساخته شود، این جلسه بسته شود، جلسهٔ تازه با دو مخزن باز شود.
   وقتی مخزن کنترل آماده شد، به‌جای اینکه به همان برگردم، یک صف چهارتایی
   کار جلویش گذاشتم. او از همان صف انتخاب کرد و دو دور دیگر روی چرتکه
   رفتیم. خودش تمامش کرد: «چرا همیشه مسیرو میبری به سمت کنجکاوی خودت؟»
   کاری که باید می‌کردم: بگویم این کارها مال جلسهٔ بعدی است، اول تحویل
   را تمام کنیم.
۱۳. **در سند نوشتم «نشانه‌های ریز تیز می‌مانند»** در حالی که همان نمونهٔ
   تأییدشده به آنها شعاع یک تا دو داده بود. سند دربارهٔ چیزی که مالک
   با چشم دیده بود اشتباه می‌گفت. خودم دور بعد پیدا و اصلاحش کردم، ولی
   اول‌بار ننوشتنش بهتر بود.

۱۴. **به برچسب «تأییدشده» در سند تکیه کردم** و گفتم قدم یک آمادهٔ
   ساخت است. مالک گفت طراحی به نقطهٔ تأیید نرسیده. برچسب در سند،
   امضای مالک نیست — فقط چیزی که مالک با چشم دیده و انتخاب کرده،
   امضاست.
۱۵. **گفتم تحقیق باید حتماً به «چیزی که ببینی» برسد.** مالک درست
   گفت: این قید مدل را از گشتن به ساختن می‌برد، و تحقیق قبلی
   می‌توانست او را به دیدن اپی در جای دیگر ببرد. شکل خروجی را از
   پیش تعیین نکن؛ فقط بگو باید قابل قضاوت باشد.

۱۶. **قاعده‌ای روی میدانی نوشتم که فقط در سند بود.** در تصمیم سه گفتم
   پیش‌فرض ارز و تقویم از `display_currency` و `display_calendar` بیاید
   که «از قبل در مدل داده هستند». در `docs/data-model.md` هستند؛ در کد
   هیچ‌کدام نیست، رکورد شخص اصلاً ساخته نشده. جلسهٔ سازنده هم همان را
   ثبت کرد، بی‌چک. یک `grep` پیدایش کرد، موقع نوشتن بریف بند دوازده.
   **سندِ مدل، خودِ مدل نیست.** قبل از نوشتن قاعده روی یک میدان، در
   کد بگرد.

۱۸. **متن کم‌رنگ کادر ورودی جلسه را پیش‌نویس مالک خواندم.** سه بار.
   آن متن پیشنهاد خودکار رابط است، نه چیزی که مالک نوشته. یک بار حتی
   در همین دفتر نوشتم «مالک داشت همان را می‌فرستاد» — غلط بود، اصلاح
   شد. متن کم‌رنگ یعنی پیشنهاد رابط. حرف مالک فقط آن است که خودش
   می‌گوید.
۱۷. **جملهٔ «وارسی خودکار یک‌ساعته گذاشتم» را در گزارش بند ده دیدم و رد
   شدم.** قاعده‌اش را می‌شناختم و می‌دانستم این مخزن CI ندارد. هشت
   بیدارشدن بی‌فایده و سقف مالک. جمله‌ای که نمی‌دانی چه هزینه‌ای دارد
   را رد نکن؛ بپرس.
۱۹. **دوباره تحقیق عمیق، این بار با عدد — و باز هفت میلیون.** بار
   قبل (بند ۹) عدد نگفته بودم. این بار گفتم و مالک سقف دو میلیون
   گذاشت؛ ابزار هفت میلیون خورد و سه پرسش از هشت را جواب داد. از دور
   اول می‌دانستم سقف را نمی‌شود به این ابزار تحمیل کرد، و باز طوری
   حرف زدم که انگار می‌شود. **سقفی که نمی‌توانی اجرا کنی سقف نیست؛
   تنها کنترل، نزدنِ دکمه است.** عدد و جزئیات در
   `research/hermes-round-1.md`، همان اول فایل.
۲۰. **سر شاتِ چسباندنِ خراب، چیزی جزئی را موضوع کردم.** مالک چاره‌ای
   جز شات ندارد و گفت وقت و هزینه‌اش سر «یه وسواس غیر مهم» می‌رود.
   قاعده‌اش در تحویل همین دور آمده: نشانهٔ روشن در دستور بگذار و شات
   را بخوان.
۲۱. **«بعد از ری‌استارت خودش بالا می‌آید» — از حافظه.** تنظیمش را
   گذاشته بودم و آزمایشش را نه. خودم در بازبینی همان شب پیدایش کردم.
   قاعدهٔ شش، یک بار دیگر.

**و یک نزدیک‌به‌اشتباه، برای درسش:** در پرسش دو با «تقویم به‌جای تب،
نمای خانه» موافقت کردم؛ بعد مالک گفت تقویم باید سال/ماه/روز داشته
باشد و برگشتم به «تب مستقل». برگشتن روی شواهد تازه درست بود. اشتباه
این بود که اول‌بار **بی‌آنکه بپرسم تقویم قرار است چه بکند** جایش را
تأیید کردم. پیش از تأیید *جا*، بپرس *چه چیزی* دارد جا می‌گیرد.

**الگوی تازه، جدا از بی‌دقتی:** اشتباه ۱۲ از جنس دیگری است. آنجا کاری
که کردم *به‌خودی‌خود* بد نبود — سندها واقعاً لازم بودند. اشتباه این بود
که **ترتیب** را عوض کردم بی‌آنکه بگویم دارم عوضش می‌کنم. صف‌گذاشتن جلوی
مالک شبیه خدمت است، ولی در عمل تصمیم‌گیری را از او می‌گیرد: او از میان
چیزهایی انتخاب می‌کند که من چیده‌ام، نه از میان چیزی که خودش خواسته
بود. **وقتی مالک نقشه‌ای گفته، صف تازه نچین — به نقشه‌اش برگرد.**

**الگو:** بیشترشان ندانستن نبود، بی‌دقتی بود. اطلاعات را داشتم و
استفاده نکردم. افت کیفیت در جلسهٔ طولانی به شکل ندانستن نمی‌آید، به شکل
سرسری‌خواندنِ چیزی که جلوی چشم است می‌آید — و از بیرون مطمئن به‌نظر
می‌رسد.

**درمانش:** قبل از هر ادعا دربارهٔ وضعیت مخزن، همان لحظه بخوان.
خروجی را نبُر. و وقتی مالک چیزی را تأیید کرد، همان‌جا منتقلش کن.

### یک نمونهٔ درست، برای تعادل

جلسهٔ سازنده گزارش داد ۱۴۲ تست سبز است. من ۱۲۲ تا شمردم. **قبل از
اینکه چیزی بگویم** رفتم مجموعهٔ تست سرور را جدا شمردم: ۲۰ تا.
۱۲۲ به‌علاوهٔ ۲۰ می‌شود ۱۴۲. تناقضی نبود.

اگر همان لحظه اعلام کرده بودم «گزارش غلط است»، هم اشتباه بود هم
اعتماد را خراب می‌کرد. **قبل از اتهام، بشمار.**

---
---

## اتفاق‌های عملیاتی که ممکن است تکرار شوند

**سوییچ مدل.** یک بار وسط کار، محافظ‌های Fable 5 پیامی را فلگ کردند و
جلسه به Opus 4.8 رفت، با برچسبی که هیچ ربطی به پروژه نداشت. سوییچ
**چسبنده** است و خودش برنمی‌گردد — مالک دستی برگرداند. پیامی که فلگ
خورد، یک دستهٔ بزرگ ویرایش چندفایلی با یک فراخوان ابزار ناقص وسطش بود.
پیام‌ها را کوچک و تک‌موضوعی نگه دار.

**جلسهٔ بی‌مخزن.** یک بار جلسه‌ای باز شد که به هیچ مخزنی وصل نبود.
خروجی چنین جلسه‌ای فقط از راه چت بیرون می‌آید — به آن بگو نتیجه را در
پاسخ چاپ کند، نه فقط در فایل.

**وارسی خودکار PR را خاموش کن — همیشه.** Claude Code وقتی جلسه PR باز
می‌کند، پیش‌فرض دارد که هر ساعت بیدار شود و وضعیت PR را چک کند، برای
CI قرمز و نظر بازبین. این مخزن CI ندارد و بازبین فقط مالک است؛ روی PR
هیچ اتفاقی نمی‌افتد جز merge خودش. جلسهٔ `1.3` بعد از `PR` بیست‌ودو
هشت بار در چهارده ساعت بیدار شد، هر بار چهار تا شش ابزار، و سهم
پنج‌ساعتهٔ مالک را بی‌هیچ کاری خورد. **هر بریف یک خط دارد: بعد از PR
بایست، هیچ وارسی خودکار.** و اگر جلسه‌ای نوشت «وارسی گذاشتم»، همان
لحظه بگو حذف کند.

**شات اپ را جلسهٔ سازنده نمی‌تواند در بدنهٔ `PR` بگذارد.** گیت‌هاب برای
تصویر در متن، میزبانی می‌خواهد و جلسه راهی به آن ندارد. در بند شانزده
همین شد: شات‌ها در چت به مالک رسیدند و بدنهٔ `PR` لینکی نداشت. این
شکست نیست، محدودیت است — ولی **در مخزن هم کامیتشان نکن** (بند ده ۱٫۳
مگابایت شات وارد مخزن کرد). قاعده: شات در پاسخ چت. در بریف‌ها همین
نوشته شود تا جلسه وقت تلف نکند.

**«وصل‌کردن مخزن به جلسه» با «اجازهٔ نوشتن» یکی نیست.** دو چیز جدا:
افزودن مخزن به منابع جلسه، و نصب اپ گیت‌هاب کلود روی آن مخزن. اولی
خواندن و کلون را کار می‌اندازد؛ بدون دومی هر `push` با خطای ۴۰۳ رد
می‌شود. اگر ناظر قرار است در مخزنی بنویسد، **اول با یک کامیت کوچک
واقعی امتحان کن**، نه بعد از نوشتن سیصد خط.

**شاخه‌ای که PRش مرج شده دوباره استفاده نمی‌شود.** از `main` تازه
شروع کن، با همان اسم شاخه، و PR تازه باز کن.

**برای سؤالی که دربارهٔ شکل داده است، شش خط بنویس و اجرا کن.** ۱۴
شهریور دو ایراد واقعی در یک بند با همین کار پیدا شد و **هر دو از
خواندن رد شده بودند** — هم از خواندنِ جلسهٔ سازنده، هم از خواندنِ ناظر.
اسکریپت یک پایگاه موقتی در حافظه ساخت، دو رکورد گذاشت، تابع را صدا زد
و خروجی را چاپ کرد. پنج دقیقه.

**چرا خواندن کم می‌آورد:** کد را می‌خوانی و شرط را درست می‌فهمی، ولی
نمی‌بینی که در دادهٔ واقعی آن شرط هیچ‌وقت برقرار نمی‌شود. یکی از آن دو
ایراد دقیقاً همین بود: شرطی که به نظر درست می‌آمد و در عمل هر
عقب‌افتاده را دور می‌ریخت.

**و همین را از جلسهٔ سازنده هم بخواه**، نه فقط خودت بکن. جملهٔ «با یک
اجرای واقعی ثابت کن» در بریف، از هر توضیحی مؤثرتر است.

**پیش از برداشتن ابزار تازه، اندازه بگیر که دردش را داری یا نه.** ۱۴
شهریور مالک ابزاری پیشنهاد داد که کد را ایندکس می‌کند تا عامل به‌جای
خواندن فایل‌به‌فایل از یک گراف بپرسد — با ادعای صد و بیست برابر توکن
کمتر. **اندازه گرفتم: کل کد ۶۰۷۷ خط بود و سندها ۱۰۶۸۰ خط.** یعنی
ابزار برای مخزنی ساخته شده که خواندنش گران است، و مال ما نبود؛ و
دانش پروژه اصلاً در کد نبود، در سند بود.

**جواب «نه» بود، ولی نکته در شکل جواب است:** نه از سر احتیاط، از سر
اندازه‌گیری — و **شرط عوض‌شدنش هم نوشته شد** («اگر کد از بیست هزار خط
رد شد، یا سرویس دومی آمد»). «نه»ی بی‌شرط، همان وسواسی است که مالک
یک بار پروژه‌اش را با آن باخت.

**در بریف بخواه که رفتار فعلی را پیش از تغییر اندازه بگیرد.** ۱۴
شهریور، بندی که چرخ را بی‌انتها می‌کرد، پیش از هر تغییری رفتار موجود را
سنجید و دید **چرخ همان موقع هم هر بار موقعیت اسکرول را عقب می‌کشید** —
ایرادی که کسی دنبالش نبود و روی گوشی حس می‌شد. اگر مستقیم می‌ساخت، آن
ایراد می‌ماند و تازه با چند برابر شدن بدتر هم می‌شد.

**مخزن می‌گوید چه ساخته شد؛ چت می‌گوید جلسه منتظر چیست.** مالک پرسید
آیا هر بار باید از چت جلسهٔ سازنده شات بگیرد یا خواندن مخزن کافی است.
اندازه گرفتم: جلسه در پایان یک `PR` دو سؤال پرسیده بود. **یکی‌شان در
متن `PR` هم بود** (اینکه ابزار تستِ کامپوننت را اضافه کند یا نه — که
خودم هم مستقل به آن رسیده بودم)، **و دیگری فقط در چت** (اینکه کدام کار
را بعد شروع کند).

پس قاعده نه «همیشه شات» است نه «هیچ‌وقت»:

- **دربارهٔ خودِ کار** — دیف، تست، کد، حتی اعتراف جلسه به ضعف خودش —
  مخزن کافی است و شات‌خواستن وقت مالک را الکی می‌گیرد.
- **دربارهٔ «حالا چه کار کنم؟»** مخزن هیچ نمی‌گوید. سؤال ترتیب کار و
  انتخاب بین دو راه، فقط در چت می‌ماند و اگر پیام بعدی جوابش را ندهد
  بی‌صدا گم می‌شود.
- **پس فقط چند خط آخرِ پیام جلسه لازم است، نه کل پاسخ**، و آن هم فقط
  پیش از اینکه مالک چیز تازه‌ای برایش بفرستد.

**و وقتی ترتیب صف را عوض می‌کنی، به جلسهٔ سازنده هم بگو.** همان روز
ترتیب کارها را با مالک عوض کردم و فقط به مالک گفتم. جلسهٔ سازنده هنوز
فکر می‌کرد کارِ قبلی در دستش است و سرِ هر تحویل می‌پرسید «حالا بروم
سراغش؟» — گرهی که هیچ‌کس نمی‌دید. **درمانش: هر بریف با یک خط شروع شود
که بگوید کجای صف است و چه چیزی عقب افتاد.**

**«منتظر بمان تا بیاید» را نگو مگر دیده باشی که در جریان است.** ۱۴
شهریور، سه نوبت پشت سر هم به مالک گفتم منتظر `PR` دوم بند بیست‌وشش
بماند. او پرسید «خب کجاست که بیاید؟» و حق داشت: نگاه کردم و **هیچ
شاخه‌ای ساخته نشده بود و هیچ `PR` بازی نبود.** آن کار اصلاً شروع نشده
بود — پیامِ شروعش در کادر تایپ مانده بود و فرستاده نشده بود. **نتیجه:
صف بی‌دلیل نیم‌ساعت خوابید و مالک منتظر چیزی ماند که وجود نداشت.**

دیدنش یک فراخوان است: شاخه‌های مخزن و `PR`های باز. **هر بار که
می‌خواهی بگویی منتظر خروجی جلسه‌ای هستیم، اول همان یک نگاه.** این همان
قاعدهٔ ششم نظارت است، فقط روی صف کار به‌جای روی کد.

**وقتی چیزی کار نمی‌کند، اول لاگ خام — بعد حدس.** هرمس به هر پیام
می‌گفت «سرویس مدل بعد از چند تلاش شکست خورد»، که هیچ نمی‌گوید. سه حدس
زدیم — کلید غلط، آدرس غلط، ظرف به اینترنت نمی‌رسد — و هر سه غلط بودند.
لاگ در **یک** قدم علت را با اسم گفت: مدل شدتِ فکرکردنی را رد می‌کرد که
قبول ندارد. **پیام خطای خام تقریباً همیشه علت را نام می‌برد؛ پیام
مؤدبانه‌ای که کاربر می‌بیند تقریباً هیچ‌وقت.** ارزش سه نوبت حدس‌زدن،
کمتر از یک بار خواندن لاگ است.

**«خطایی ننوشت» یعنی «خطایی ننوشت»، نه «کار کرد».** بعد از تست اپ روی
گوشی، مالک گفت هیچ خطایی نداد. به‌جای پرسیدن دوباره از او، از لاگ سرور
و پایگاه داده اندازه گرفتیم — هم ثابت شد همگام‌سازی کار می‌کند، هم یک
ایراد پیدا شد که هیچ‌کس ندیده بود. **شاهد را از ماشین بگیر، نه از
کاربر.**

**سند مرجع می‌تواند با خودش تناقض پیدا کند.** در سند هویت، متن گفت
ارتفاع خانه ۶۶ است ولی بلوک کپی‌پیستِ آخر سند هنوز `50px` داشت — و آن
بلوک دقیقاً همان نیمه‌ای است که ساخته شده که کپی شود. یعنی جلسهٔ
سازندهٔ بعدی بی‌سروصدا بند نه را برمی‌گرداند. پیدا و اصلاح شد.
**روش بررسی:** همهٔ `--نام: مقدار;`ها از بلوک سند و از
`app/src/tokens.css` استخراج و مقایسه شد — صفر اختلاف.

**پیست چندخطی روی دستگاه مالک، خط‌ها را به هم می‌چسباند.** ۱۴ شهریور
دستوری با `heredoc` فرستاده شد؛ در ترمینال مالک همهٔ خط‌ها به یک خط
تبدیل شدند، پایان‌بخشِ `heredoc` هیچ‌وقت سرِ خط خودش نیامد و شل روی
`>` ماند. هیچ‌چیز اجرا نشد و مالک باید دستی آزادش می‌کرد.

**قاعده: هر دستوری که به مالک می‌دهی باید با چسبیدن خط‌ها هم درست
اجرا شود.** یعنی آخر هر خط `;` بگذار، و در دستور پیستی هیچ‌وقت
`heredoc` نگذار.

برای ساختن فایل چندخطی روی سرور، دو راه امتحان‌شده:

- `printf '%s\n' 'خط' 'خط' > file` — وقتی محتوا نقل‌قول تک ندارد.
- `printf %s '<base64>' | base64 -d > file` — وقتی محتوا نقل‌قول دارد
  یا پیچیده است. یک نشانهٔ پیوسته، بی‌فاصله و بی‌نقل‌قول؛ از هر
  به‌هم‌ریختنی سالم بیرون می‌آید.

**و نصب را به یک وارسی نحوی گره بزن** (`sh -n file && push`)، تا اگر
پیست نصفه رسید بلند شکست بخورد، نه اینکه فایل خراب نصب شود.

**خروجی برنامه‌ای که مالک باید بخواند، باید تک‌زبانه باشد.** جواب خامِ
`JSON` درِ چرتکه روی ترمینال مالک خوانا نبود: نام فیلدها لاتین و
مقدارها فارسی، در یک خط. **پس قالب‌بندی کارِ برنامه است، نه کارِ
مدل** — برنامه خطِ آمادهٔ فارسی چاپ می‌کند و مدل فقط تکرارش می‌کند.
هم خوانا می‌شود، هم با قاعدهٔ ثابت پروژه می‌خواند که مدل عددی
نمی‌سازد که با آن حساب کنیم.

**قابلیتی را رد نکن پیش از اینکه اندازه‌اش گرفته باشی.** ۱۴ شهریور مالک
پرسید کاربرها می‌توانند از هرمس استفاده کنند یا نه، و ناظر گفت نه و سه
دلیل هم آورد. بعد تحقیق شد و **هر سه راه‌حل داشتند**. مالک درست گفت:
«قبل از اینکه چیزی بدونی دنبال جلوگیری ازش هستی.»

**قاعده: وقتی جوابت «نمی‌شود» است، اول بگو با چه شاهدی.** اگر شاهدی
نداری، جوابِ درست «نمی‌دانم، می‌روم ببینم» است. «نه»ی بی‌اندازه‌گیری،
کار نکرده است — و بدتر از دیر جواب‌دادن است، چون راه را می‌بندد.

**کد خروجی یک ابزار، شاهدِ سالم‌بودن نتیجه‌اش نیست.** `tar` با کد ۱
برگشت («فایل زیر دستم عوض شد») و نگهبانِ ناظر کل کار را متوقف کرد، در
حالی که بسته سالم بود. دفعهٔ بعد **خودِ بسته باز و شمرده شد** — چند فایل
دارد، آیا پیکربندی و حافظه و پایگاه وضعیت توش هست — و همان درست بود.
عکسش هم صادق است: کد صفر ثابت نمی‌کند محتوا کامل است.

**قبل از فرستادن دستور بعدی، خروجی‌ای را که همین حالا داری بخوان.**
مالک گفت هرمس جواب نمی‌دهد و ناظر یک دستور تشخیص فرستاد — در حالی که
شات قبلی نشان می‌داد همه‌چیز موفق بوده و دروازه بالاست. **دستور اضافه
هزینهٔ مالک است، نه هزینهٔ ناظر.**

**پیش از برداشتن نسخهٔ پشتیبان از پوشهٔ خانگی یک برنامه، ببین داخلش چیست.**
نسخهٔ اول هرمس ۴۰۹ مگابایت شد و دیسک را از ۶۱٪ به ۹۰٪ برد. اندازه‌گیری
نشان داد ۱٫۴ گیگ از ۱٫۴ گیگ، پوشهٔ نصب و زمان اجراست — دوباره‌دانلودشدنی.
چیزی که واقعاً جایگزین‌ناپذیر بود، **۱٫۸ مگابایت** درآمد.

**و برنامه‌ای که وسط نسخه‌برداری در حال نوشتن است، نسخهٔ سالم نمی‌دهد.**
چند ثانیه خواباندنش ارزان‌تر از یک پایگاه دادهٔ نیمه‌نوشته است.
